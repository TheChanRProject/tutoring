var name = "Alex";
var beerQuantity = 50;
var clause = "is going to the store to buy";
var beer = "beers";

// To change a number to a string, add .toString() after the variable

// An example is shown below:

function example() {
  console.log("Jim is" + ' ' + beerQuantity.toString() + ' ' + "years old." );
}
example();

// See result in the terminal


// Your Tasks:
//  Write a function that will display in the Terminal using only variable names :
// Alex is going to the store to buy 50 beers.
